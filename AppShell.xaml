<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="SRQCC.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:SRQCC" 
    Title="Santa Rosa de Quives Country Club"
    FlyoutBackgroundColor="#FFFFFF"
    FlyoutHeaderBehavior="Fixed">

    <!-- ✅ Flyout Header Mejorado -->
    <Shell.FlyoutHeader>
        <Border 
        BackgroundColor="#059669"
        Stroke="Transparent">
            <Grid 
            BackgroundColor="#059669" 
            HeightRequest="325"
            Padding="20,30,20,20">

                <VerticalStackLayout 
                HorizontalOptions="Center" 
                VerticalOptions="Center"
                Spacing="5">

                    <!-- Logo MUY GRANDE -->
                    <Image
                    Source="srq_logo.png"
                    HeightRequest="300"
                    WidthRequest="300"
                    HorizontalOptions="Center"
                    Aspect="AspectFit" />


                </VerticalStackLayout>
            </Grid>
        </Border>
    </Shell.FlyoutHeader>

    <!-- Items del Menú con texto emoji (si no tienes los PNG) -->
    <FlyoutItem Title="🏠 Inicio">
        <Tab>
            <ShellContent 
        Route="MainPage"
        ContentTemplate="{DataTemplate local:MainPage}" />
        </Tab>
    </FlyoutItem>

    <FlyoutItem Title="👤 Mi Perfil">
        <Tab>
            <ShellContent 
        Route="MiPerfil"
        ContentTemplate="{DataTemplate local:MiPerfil}" />
        </Tab>
    </FlyoutItem>
    
    <FlyoutItem Title="💸 Pagos en linea">
        <Tab>
            <ShellContent 
        Route="PagosRedirect"
        ContentTemplate="{DataTemplate local:PagosRedirectPage}" />
        </Tab>
    </FlyoutItem>

    <FlyoutItem Title="💳 Mis Pagos">
        <Tab>
            <ShellContent 
        Route="MisPagos"
        ContentTemplate="{DataTemplate local:MisPagos}" />
        </Tab>
    </FlyoutItem>

    <FlyoutItem Title="📞 Contáctanos">
        <Tab>
            <ShellContent 
        Route="ContactanosPage"
        ContentTemplate="{DataTemplate local:ContactanosPage}" />
        </Tab>
    </FlyoutItem>


    




    <!-- ✅ Footer Mejorado -->
    <Shell.FlyoutFooter>
        <Border 
        BackgroundColor="#F8FAFC"
        Stroke="Transparent"
        StrokeThickness="0">

            <VerticalStackLayout Padding="20,15" Spacing="12">
                <Grid>
                    <Label
                        Text="{Binding AppVersion}"
                        FontSize="11"
                        VerticalOptions="Center" />
                </Grid>
                
                <!-- Botón Cerrar Sesión - Toda la fila clickeable -->
                <Grid ColumnDefinitions="*,Auto" VerticalOptions="Center">
                    <Grid.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnLogoutClicked" />
                    </Grid.GestureRecognizers>

                    <Label
                    Grid.Column="0"
                    Text="Cerrar Sesión"
                    TextColor="#DC2626"
                    FontSize="16"
                    FontAttributes="Bold"
                    VerticalOptions="Center" />

                    <Label
                    Grid.Column="1"
                    Text="🚪"
                    TextColor="#DC2626"
                    FontSize="18"
                    VerticalOptions="Center" />
                </Grid>

            </VerticalStackLayout>
        </Border>
    </Shell.FlyoutFooter>

</Shell>
