<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SRQCC.ContactanosPage"
             Title="Contactanos"
             BackgroundColor="#F8FAFC">


    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="25">

            <!-- Redes Sociales -->
            <Border
            Padding="20"
            BackgroundColor="White"
            Stroke="Transparent"
            StrokeShape="RoundRectangle 15">

                <VerticalStackLayout Spacing="15">
                    <Label
                    FontAttributes="Bold"
                    FontSize="18"
                    Text="🌐 Síguenos en"
                    TextColor="#2D3748" />

                    <HorizontalStackLayout 
                    HorizontalOptions="Center" 
                    Spacing="20">

                        <!-- Facebook -->
                        <Border
                        Padding="0"
                        BackgroundColor="Transparent"
                        Stroke="Transparent"
                        StrokeShape="RoundRectangle 10">
                            <ImageButton
                            Source="facebook.png"
                            HeightRequest="44"
                            WidthRequest="44"
                            BackgroundColor="Transparent"
                            Clicked="OnFacebookClicked" />
                        </Border>

                        <!-- Instagram -->
                        <Border
                        Padding="0"
                        BackgroundColor="Transparent"
                        Stroke="Transparent"
                        StrokeShape="RoundRectangle 10">
                            <ImageButton
                            Source="instagram.png"
                            HeightRequest="44"
                            WidthRequest="44"
                            BackgroundColor="Transparent"
                            Clicked="OnInstagramClicked" />
                        </Border>

                        <!-- YouTube -->
                        <Border
                        Padding="0"
                        BackgroundColor="Transparent"
                        Stroke="Transparent"
                        StrokeShape="RoundRectangle 10">
                            <ImageButton
                            Source="youtube.png"
                            HeightRequest="44"
                            WidthRequest="44"
                            BackgroundColor="Transparent"
                            Clicked="OnYouTubeClicked" />
                        </Border>

                        <!-- Tiktok -->
                        <Border
                        Padding="0"
                        BackgroundColor="Transparent"
                        Stroke="Transparent"
                        StrokeShape="RoundRectangle 10">
                            <ImageButton
                            Source="tiktok.png"
                            HeightRequest="44"
                            WidthRequest="44"
                            BackgroundColor="Transparent"
                            Clicked="OnTikTokClicked" />
                        </Border>

                        <!-- Linktree -->
                        <Border
                        Padding="0"
                        BackgroundColor="Transparent"
                        Stroke="Transparent"
                        StrokeShape="RoundRectangle 10">
                            <ImageButton
                            Source="linktree.png"
                            HeightRequest="44"
                            WidthRequest="44"
                            BackgroundColor="Transparent"
                            Clicked="OnLinkTreeClicked" />
                        </Border>

                    </HorizontalStackLayout>

                    <!-- Texto descriptivo opcional -->
                    <Label
                    Text="Mantente conectado con nosotros"
                    FontSize="12"
                    HorizontalOptions="Center"
                    TextColor="#718096" />

                </VerticalStackLayout>
            </Border>

            <!-- Teléfono -->
            <Border
            Padding="20"
            BackgroundColor="White"
            Stroke="Transparent"
            StrokeShape="RoundRectangle 15">

                <VerticalStackLayout Spacing="15">
                    <Label
                    FontAttributes="Bold"
                    FontSize="18"
                    Text="📞 Telefono"
                    TextColor="#2D3748" />

                    <VerticalStackLayout Spacing="12">
                        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="10">
                            <VerticalStackLayout Grid.Column="0">
                                <Label Text="Atención al asociado" TextColor="#4A5568" FontSize="14"/>
                                <Label Text="+51984703188" FontAttributes="Bold" TextColor="#2D3748" FontSize="16"/>
                            </VerticalStackLayout>
                            <Button 
                            Grid.Column="1"
                            Text="📋"
                            BackgroundColor="#25D366"
                            TextColor="White"
                            Padding="15,10"
                            CornerRadius="20"
                            Clicked="OnCopiarTelefonoClicked"/>
                        </Grid>

                        <Label Text="🕒 Horario: " 
                    FontSize="12" TextColor="#718096" Margin="0,5,0,0"/>
                        <Label Text="            Lunes a Viernes 9:00 AM - 6:00 PM" 
                    FontSize="12" TextColor="#718096" Margin="0,5,0,0"/>
                        <Label Text="            Sabados 9:00 AM - 2:00 PM" 
                    FontSize="12" TextColor="#718096" Margin="0,5,0,0"/>
                    </VerticalStackLayout>
                </VerticalStackLayout>
            </Border>

            <!-- Correo -->
            <Border
            Padding="20"
            BackgroundColor="White"
            Stroke="Transparent"
            StrokeShape="RoundRectangle 15">

                <VerticalStackLayout Spacing="15">
                    <Label
                    FontAttributes="Bold"
                    FontSize="18"
                    Text="✉️ Correo Electrónico"
                    TextColor="#2D3748" />

                    <VerticalStackLayout Spacing="12">
                        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="10">
                            <VerticalStackLayout Grid.Column="0">
                                <Label Text="Atención al asociado" TextColor="#4A5568" FontSize="14"/>
                                <Label Text="atencionalasociado@srq-cc.com" FontAttributes="Bold" TextColor="#2D3748" FontSize="16"/>
                            </VerticalStackLayout>
                            <Button 
                            Grid.Column="1"
                            Text="📋"
                            BackgroundColor="#25D366"
                            TextColor="White"
                            Padding="15,10"
                            CornerRadius="20"
                            Clicked="OnCopiarCorreoClicked"/>
                        </Grid>
                    </VerticalStackLayout>
                </VerticalStackLayout>
            </Border>

            <!-- WhatsApp -->
            <Border
            Padding="20"
            BackgroundColor="White"
            Stroke="Transparent"
            StrokeShape="RoundRectangle 15">

                <VerticalStackLayout Spacing="15">
                    <Label
                    FontAttributes="Bold"
                    FontSize="18"
                    Text="💬 WhatsApp"
                    TextColor="#2D3748" />

                    <VerticalStackLayout Spacing="10">
                        <Label Text="Atención al asociado" 
                           FontSize="14" TextColor="#4A5568"/>
                        <Label Text="Chatea directamente con nosotros" 
                           FontSize="12" TextColor="#718096"/>

                        <Button 
                        Text="Abrir WhatsApp"
                        BackgroundColor="#25D366"
                        TextColor="White"
                        Padding="20,12"
                        CornerRadius="10"
                        HorizontalOptions="Center"
                        Clicked="OnAbrirWhatsAppClicked"/>
                    </VerticalStackLayout>
                </VerticalStackLayout>
            </Border>



            <!-- Dirección Club-->
            <Border
            Padding="20"
            BackgroundColor="White"
            Stroke="Transparent"
            StrokeShape="RoundRectangle 15">

                <VerticalStackLayout Spacing="15">
                    <Label
                    FontAttributes="Bold"
                    FontSize="18"
                    Text="📍 Dirección Club"
                    TextColor="#2D3748" />

                    <VerticalStackLayout Spacing="10">
                        <Label Text="Santa Rosa de Quives Country Club" 
                           FontSize="14" TextColor="#2D3748" FontAttributes="Bold"/>
                        <Label Text="Carretera Lima – Canta Km. 65, Quives 15335" 
                           FontSize="14" TextColor="#2D3748" FontAttributes="Bold"/>
                        <Label Text="Lima, Perú" 
                           FontSize="14" TextColor="#2D3748"/>

                        <Button 
                        Text="🗺️ Ver en Google Maps"
                        BackgroundColor="#25D366"
                        TextColor="White"
                        Padding="20,12"
                        CornerRadius="10"
                        HorizontalOptions="Center"
                        Clicked="OnAbrirMapsClicked"/>
                    </VerticalStackLayout>
                </VerticalStackLayout>
            </Border>

            <!-- Dirección Oficina-->
            <Border
            Padding="20"
            BackgroundColor="White"
            Stroke="Transparent"
            StrokeShape="RoundRectangle 15">

                <VerticalStackLayout Spacing="15">
                    <Label
                    FontAttributes="Bold"
                    FontSize="18"
                    Text="📍 Dirección Oficina"
                    TextColor="#2D3748" />

                    <VerticalStackLayout Spacing="10">
                        <Label Text="Santa Rosa de Quives Country Club" 
                        FontSize="14" TextColor="#2D3748" FontAttributes="Bold"/>
                        <Label Text="Av. Carlos Izaguirre 1153, Los Olivos" 
                        FontSize="14" TextColor="#2D3748" FontAttributes="Bold"/>
                        <Label Text="Lima, Perú" 
                        FontSize="14" TextColor="#2D3748"/>

                        <Button 
                        Text="🗺️ Ver en Google Maps"
                        BackgroundColor="#25D366"
                        TextColor="White"
                        Padding="20,12"
                        CornerRadius="10"
                        HorizontalOptions="Center"
                        Clicked="OnAbrirMapsOficinaClicked"/>
                    </VerticalStackLayout>
                </VerticalStackLayout>
            </Border>


        </VerticalStackLayout>
    </ScrollView>
    
</ContentPage>